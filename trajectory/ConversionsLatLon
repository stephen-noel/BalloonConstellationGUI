%turning HTML code into Matlab to convert m to deg for lat/long


function ConversionsLatLon(obj) %Compute lengths of degrees
        rad = deg*((2.0*Math.PI)/360.0); %Converts degrees to radians

		       

		%Set up "Constants"
		m1 = 111132.92;		% latitude calculation term 1
		m2 = -559.82;		% latitude calculation term 2
		m3 = 1.175;			% latitude calculation term 3
		m4 = -0.0023;		% latitude calculation term 4
		p1 = 111412.84;		% longitude calculation term 1
		p2 = -93.5;			% longitude calculation term 2
		p3 = 0.118;			% longitude calculation term 3

		%Calculate the length of a degree of latitude and longitude in meters
		latlen = m1 + (m2 * cos(2 * lat)) + (m3 * cos(4 * lat)) + (m4 * cos(6 * lat));
		longlen = (p1 * cos(lat)) + (p2 * cos(3 * lat)) +(p3 * cos(5 * lat));
end
        
 output(latlen); %lat in m
 output(longlen); %long in m
